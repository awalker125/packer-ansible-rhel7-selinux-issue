packer-ansible-rhel7-selinux-issue
=============================================

This project is a simple test harness to recreate an issue where packer and ansible cannot copy files into a container.